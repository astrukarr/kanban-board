{
  "name": "kanban-board-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "e2e": "turbo run e2e",
    "typecheck": "turbo run typecheck",
    "start": "cd apps/kanban && npm run start"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  }
}
